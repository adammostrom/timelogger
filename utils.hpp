#ifndef UTILS_H   // no dot
#define UTILS_H

/**
 * Utilities.
 */

long calculate_hour_from_seconds(long seconds);
long calculate_mins_from_seconds(long seconds);
long calculate_secs_from_hour_min(long hour, long min);

#endif // UTILS_H